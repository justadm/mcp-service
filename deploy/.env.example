TRAEFIK_ACME_EMAIL=admin@justgpt.ru

DOMAIN_APP=app.justgpt.ru
DOMAIN_API=api.justgpt.ru
DOMAIN_MCP=mcp.justgpt.ru

# Per-project Bearer tokens for MCP transport (nginx path остаётся под общим Basic Auth).
MCP_P1_BEARER_TOKEN=change_me_p1
MCP_P2_BEARER_TOKEN=change_me_p2
MCP_PG_BEARER_TOKEN=change_me_pg
MCP_TW_BEARER_TOKEN=change_me_tw

# Если включишь auth для Timeweb Cloud API в `deploy/projects/tw.yml`:
# TIMEWEB_CLOUD_TOKEN=change_me_timeweb
