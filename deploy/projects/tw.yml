server:
  name: mcp-service
  version: 0.1.0

transport:
  type: http
  host: 0.0.0.0
  port: 8080
  path: /p/tw/mcp
  stateful: true
  auth:
    type: bearer
    tokenEnv: MCP_BEARER_TOKEN

sources:
  - id: timeweb
    type: openapi
    specFile: examples/timeweb.openapi.json
    baseUrl: https://api.timeweb.cloud
    # Чтобы включить авторизацию в Timeweb Cloud API:
    # auth:
    #   type: bearer
    #   tokenEnv: TIMEWEB_TOKEN
    auth:
      type: bearer
      tokenEnv: TIMEWEB_TOKEN
